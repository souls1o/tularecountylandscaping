# Copy to `.env.local` (do not commit secrets).

# Telegram — estimate form posts to your bot via sendMessage
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Upstash Redis — optional; enables “Top pages” ranking by real traffic
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
